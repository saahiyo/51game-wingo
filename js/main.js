import { initGameListEvents } from './events.js';
import { getGamePeriod } from './utils.js';

document.addEventListener('DOMContentLoaded', () => {
    initGameListEvents(); // Initialize game list events
});
